<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thefirstlineofcode.sand.server.lite.concentrator.ConcentrationMapper">
    <insert id="insert" parameterType="com.thefirstlineofcode.sand.server.lite.concentrator.Concentration">
        INSERT INTO CONCENTRATIONS(id, concentrator, node, lan_id, communication_net, address, confirmation_time) VALUES(#{id}, #{concentrator}, #{node}, #{lanId}, #{communicationNet}, #{address}, #{confirmationTime})
    </insert>
    
	<select id="selectCountByConcentratorAndNode" resultType="int">
		SELECT count(*) FROM CONCENTRATIONS WHERE concentrator = #{arg0} AND node = #{arg1}
	</select>
	
	<select id="selectCountByConcentratorAndLanId" resultType="int">
		SELECT count(*) FROM CONCENTRATIONS WHERE concentrator = #{arg0} AND lan_id = #{arg1}
	</select>
	
	<select id="selectByConcentratorAndNode" resultType="com.thefirstlineofcode.sand.server.lite.concentrator.Concentration">
		SELECT * FROM CONCENTRATIONS WHERE concentrator = #{arg0} AND node = #{arg1}
	</select>
	
	<select id="selectByConcentratorAndLanId" resultType="com.thefirstlineofcode.sand.server.lite.concentrator.Concentration">
		SELECT * FROM CONCENTRATIONS WHERE concentrator = #{arg0} AND lan_id = #{arg1}
	</select>
	
	<select id="selectByLanId" resultType="com.thefirstlineofcode.sand.server.concentrator.Node">
		SELECT node AS device_id, lan_id, communication_net, address FROM CONCENTRATIONS WHERE concentrator = #{arg0} AND lan_id = #{arg1}
	</select>
</mapper>
