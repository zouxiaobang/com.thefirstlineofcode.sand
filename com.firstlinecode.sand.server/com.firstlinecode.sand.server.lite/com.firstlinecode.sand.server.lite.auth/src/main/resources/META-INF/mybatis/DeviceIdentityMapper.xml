<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firstlinecode.sand.server.lite.auth.DeviceIdentityMapper">
    <insert id="insert" parameterType="com.firstlinecode.sand.server.lite.auth.D_DeviceIdentity">
        INSERT INTO DEVICE_IDENTITY(id, device_id, jid, credentials, authorization_id, registered_time) VALUES(#{id}, #{deviceId}, #{jid}, #{credentials}, #{authorizationId}, #{registeredTime})
    </insert>
    
   <update id="delete">
        DELETE FROM DEVICE_IDENTITY WHERE jid=#{0, typeHandler=JabberIdTypeHandler}
    </update>
    
	<select id="selectByDeviceId" resultType="DeviceIdentity">
		SELECT * FROM DEVICE_IDENTITY WHERE device_id=#{0}
    </select>
    
	<select id="selectByJid" resultType="DeviceIdentity">
		SELECT * FROM DEVICE_IDENTITY WHERE jid=#{0, typeHandler=JabberIdTypeHandler}
    </select>
    
	<select id="selectCountByDeviceId" resultType="int">
		SELECT COUNT(*) FROM DEVICE_IDENTITY WHERE device_id=#{0}
    </select>
    
	<select id="selectCountByJid" resultType="int">
		SELECT COUNT(*) FROM DEVICE_IDENTITY WHERE jid=#{0, typeHandler=JabberIdTypeHandler}
    </select>
</mapper>
