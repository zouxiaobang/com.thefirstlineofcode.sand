<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firstlinecode.sand.server.lite.auth.DeviceIdentityMapper">
    <insert id="insert" parameterType="DeviceIdentity">
        INSERT INTO DEVICE_IDENTITY(id, device_id, jid, credentials, state=#{1, typeHandler=org.apache.ibatis.type.EnumTypeHandler}) VALUES(#{id}, #{deviceId}, #{jid}, #{credentials}, #{state})
    </insert>
    
    <insert id="updateDeviceIdentityState">
		UPDATE DEVICE_IDENTITY SET state=#{1, typeHandler=org.apache.ibatis.type.EnumTypeHandler} WHERE device_id=#{0}
    </insert>
    
   <delete id="delete">
        DELETE FROM DEVICE_IDENTITY WHERE device_id=#{0, typeHandler=JabberIdTypeHandler}
    </delete>
    
	<select id="selectByJid" resultType="DeviceIdentity">
		SELECT * FROM DEVICE_IDENTITY WHERE jid=#{0, typeHandler=JabberIdTypeHandler}
    </select>
    
	<select id="selectCountByJid" resultType="int">
		SELECT COUNT(*) FROM DEVICE_IDENTITY WHERE jid=#{0, typeHandler=JabberIdTypeHandler}
    </select>
</mapper>
