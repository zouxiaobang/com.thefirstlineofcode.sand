<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firstlinecode.sand.server.lite.auth.DeviceAuthorizationMapper">
    <insert id="insert" parameterType="DeviceAuthorization">
        INSERT INTO DEVICE_AUTHORIZATION(id, device_id, authorizer, authorize_time, expired_time) VALUES(#{id}, #{deviceId}, #{authorizer}, #{authorize_time}, #{expired_time})
    </insert>
</mapper>
